import { computed, observable } from "mobx";
import * as TwitterService from "../services/twitter";
import * as EpiService from "../services/twitter";
import getTweets from "../services/twitter";

class Twitter {
  // TwitterService.getTweets('blah').then(response => {
  //     return response;
  // });
}

export default Twitter;
